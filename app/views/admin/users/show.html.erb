<div class="max-w-2xl mx-auto mt-8">
  <div class="bg-white rounded-2xl shadow p-8 mb-6">
    <h2 class="text-xl font-semibold text-slate-700 mb-4">Détail utilisateur</h2>
    <p class="mb-2"><span class="font-bold">Email :</span> <%= @user.email %></p>
    <p class="mb-2"><span class="font-bold">Rôles :</span> <%= @user.roles.join(', ') %></p>
  </div>
  <%= link_to 'Retour', admin_users_path, class: 'inline-block px-4 py-2 bg-[#C8102E] text-white text-sm rounded-lg hover:bg-[#FFB200] transition' %>
</div>
