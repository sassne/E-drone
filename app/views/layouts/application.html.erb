<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "E Drone" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if controller_name == 'sessions' && action_name == 'new' || (controller_path == 'devise/sessions' && action_name == 'new') %>
      <main class="min-h-screen flex items-center justify-center bg-gray-100">
        <div class="w-full max-w-md p-8 bg-white rounded-lg shadow-md">
          <%= render "shared/flash" %>
          <%= yield %>
        </div>
      </main>
    <% else %>
      <div data-controller="sidebar" class="min-h-screen bg-gray-100">
        <%= render "shared/flash" %>
        <!-- Sidebar backdrop -->
        <div
          data-sidebar-target="backdrop"
          class="fixed inset-0 z-40 bg-black bg-opacity-50 lg:hidden"
          data-action="click->sidebar#toggle"
        ></div>

        <!-- Mobile sidebar -->
        <div
          data-sidebar-target="sidebar"
          class="fixed inset-y-0 left-0 z-50 w-64 transform -translate-x-full bg-white shadow-lg transition-transform duration-300 ease-in-out lg:hidden"
        >
          <%= render "shared/sidebar" %>
        </div>

        <!-- Desktop sidebar -->
        <div class="hidden lg:fixed lg:inset-y-0 lg:flex lg:w-64 lg:flex-col">
          <%= render "shared/sidebar" %>
        </div>
        <div class="lg:pl-64">
          <%= yield %>
        </div>
      </div>
    <% end %>
  </body>
</html>
